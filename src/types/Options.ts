export type Options = {
  spacing: number,
  numCircles: number,
  minRadius: number,
  maxRadius: number,
  higherAccuracy: boolean,
  colors: string | Array<string>,
  minAlpha: number,
  background: string | boolean,
  useMainThread: boolean,
  reuseWorker: boolean
}
